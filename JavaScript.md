# JavaScript

## 1. Async/Await

It's a modern way of making asynchronous code look like synchronous code

## 2. Run typescript files using node

First compile typescript file using this command : tsc app.ts <br>
Then an app.js file will be created; run that file using : node app.js

## 3. Destructuring

To destructure an array use [x , y] and to destructure an object use {x, y}.
